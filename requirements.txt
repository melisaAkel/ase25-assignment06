# requirements.txt

# Core framework
Flask==3.0.0

# Flask dependencies (usually installed automatically, but listed explicitly for TA safety)
Werkzeug==3.0.1
Jinja2==3.1.3
itsdangerous==2.1.2
click==8.1.7

# SQLite is part of Python standard library → NO sqlite package needed
# datetime, os, hashlib, typing, etc. are also standard library

# Optional (recommended for development)
python-dotenv==1.0.1
